# test_invalid.yaml (test_invalid.yaml)

q_states:
  q_list:
    - q0
    - q1
  initial: q0
  final: q_invalid # Estado que no existe

alphabet:
  - a

tape_alphabet:
  - b # Falta 'a' que está en alphabet

delta:
  - params:
      initial_state: q0
      tape_input: a
    output:
      final_state: q2 # Estado que no existe
      tape_output: b
      tape_displacement: X # Movimiento inválido

simulation_strings:
  - abc # 'c' no está en el alfabeto
